target-version = "py312"
extend-exclude = [".venv", "venv", "staticfiles", "media"]

[lint]
# Existing code has a large style backlog. Keep CI focused on syntax and
# undefined-name errors; broaden this set incrementally as the backlog is fixed.
select = ["E9", "F63", "F7", "F82"]
